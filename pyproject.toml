[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "PyMSM"
authors = [{name = "Hagai Rossman, Ayya Keshet", email = "hagairossman@gmail.com"}]
dynamic = ["version", "description"]
